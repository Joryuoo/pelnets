---
created: 2026-09-09 00:20
status: "#philnits"
tags:
  - algorithms/2022
  - programming/2022
  - data-encoding/2022
  - year/2022
---

# 2022S_FE-B_6

![[2022S_FE-B_Q6_p1.png]]
![[2022S_FE-B_Q6_p2.png]]
![[2022S_FE-B_Q6_p3.png]]
![[2022S_FE-B_Q6_p4.png]]
![[2022S_FE-B_Q6_p5.png]]
?
**Subquestion 1:**
- A: c) `(Data & Mask) = Mask`
- B: a) `(Data & Mask) = '00000000'B`
- C: d) never returns the return value 1
- D: e) always returns the return value 2
- E: b) Change proposal (2)

**Subquestion 2:**
- F: c) `Work | '00000001'B`
- G: b) `Work & '11111110'B`
- H: a) 6
- I: d) `01100100`

### Explanation

**Subquestion 1: Bitwise Tests**
1. **Blank A (Rv = 2):** A return value of 2 means **all tested bits are 1's**. The bits tested are where `Mask` has 1s. If we perform `Data & Mask`, the result must exactly equal `Mask` for all tested bits to be 1. Thus, **A = c**.
2. **Blank B (Rv = 0):** A return value of 0 means **all tested bits are 0's**. If all bits in `Data` corresponding to 1s in `Mask` are 0, then `Data & Mask` will result in all 0s. Thus, **B = a**.
3. **Blank C:** If `Mask` has only one bit set to 1, then the tested bit in `Data` is either 0 or 1. It is impossible to have a "mix of 0's and 1's" when testing a single bit. Therefore, it **never returns the return value 1** (which represents mixed bits). Thus, **C = d**.
4. **Blank D:** If `Data` and `Mask` are identical, then `Data & Mask` is equivalent to `Mask & Mask`, which equals `Mask`. This triggers the condition for `Rv = 2`. Thus, it **always returns the return value 2**. Thus, **D = e**.
5. **Blank E:** If `Mask` is `'00000000'B`, `Data & Mask` evaluates to `'00000000'B`. 
   - In Change proposal (1), condition `A` (`'00000000'B == '00000000'B`) evaluates to true first, improperly returning 2. 
   - In Change proposal (2), condition `B` (`'00000000'B == '00000000'B`) is evaluated first, correctly returning 0. Thus, **E = b**.

**Subquestion 2: Bit Counting Algorithm**
1. **Blanks F & G (Checking if rightmost bit is 1):** 
   - If the rightmost bit of `Work` is 1, doing a bitwise OR with `'00000001'B` will not change its value. Hence, `Work | '00000001'B == Work`. Thus, **F = c**.
   - Doing a bitwise AND with `'11111110'B` will clear the rightmost bit, guaranteeing it changes its value. Hence, `Work & '11111110'B != Work`. Thus, **G = b**.
2. **Blank H (Minimum processing time for Program 3):** 
   The minimum processing time occurs when `Data` is `'00000000'B`. 
   Tracing the execution:
   - `Work <- Data` (1)
   - `Count <- 0` (1)
   - `WHILE (Work != '00000000'B)` evaluates to False (2)
   - `return Count` (2)
   Total minimum time = $1 + 1 + 2 + 2 = 6$. Thus, **H = a**.
3. **Blank I (Bit replacement efficiency):**
   - Let `Work` = `'01100110'B`
   - `Work - 1` = `'01100101'B`
   - `Work & (Work - 1)` = `'01100100'B`. 
   This operation cleverly clears the least significant 1-bit in `Work`. Thus, **I = d**.

---
