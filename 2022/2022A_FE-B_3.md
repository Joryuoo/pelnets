---
created: 2026-09-08 22:35
status: "#philnits"
tags:
  - information-management/2022
  - software/2022
  - year/2022
---

# 2022A_FE-B_3

![[2022A_FE-B_Q3_p1.png]]
![[2022A_FE-B_Q3_p2.png]]
![[2022A_FE-B_Q3_p3.png]]
![[2022A_FE-B_Q3_p4.png]]
?
Subquestion 1: A: c) P.Quantity, B: e) SUM(D.Quantity), C: b) LEFT OUTER JOIN
Subquestion 2: D: e) UPDATE Employee, E: d) O.OrderID = D.OrderID, F: c) GROUP BY O.EmployeeID HAVING SUM(D.Amount) > 10000

### Explanation

**Subquestion 1**
The query calculates the `EstimateQuantity`, which is the currently stocked quantity minus the quantity to be delivered.
* **Blank C:** The `Product` table must be joined with the `ExtractedOrderDetail` table. Since a product might not have any delivery requests, an `INNER JOIN` would exclude products with 0 orders. A `LEFT OUTER JOIN` ensures all products are included, even if they have no corresponding rows in `ExtractedOrderDetail`. (Correct Answer: **b**)
* **Blank A:** The stocked quantity is stored in the `Product` table as `Quantity`. Since the query groups by `P.ProductID, P.ProductName, P.Quantity`, we can select `P.Quantity` directly. (Correct Answer: **c**)
* **Blank B:** The total quantity to be delivered for a product is the sum of quantities from all its orders. This is calculated as `SUM(D.Quantity)`. The `CASE` statement ensures that if `SUM(D.Quantity)` is `NULL` (which happens for products with no orders due to the `LEFT JOIN`), it is treated as `0`. (Correct Answer: **e**)

---

**Subquestion 2**
The query increases the salary of employees meeting a specific condition.
* **Blank D:** The statement modifies existing rows in the `Employee` table. The correct SQL command for modifying data is `UPDATE`. Thus, the statement should begin with `UPDATE Employee`. (Correct Answer: **e**)
* **Blank E:** The inner subquery joins `Order O` and `OrderDetail D`. While the query provided oddly uses the `ON` clause for the correlation condition (`ON O.EmployeeID = Employee.EmployeeID`), the actual tables `Order` and `OrderDetail` still need a join condition. This join condition must be placed in the `WHERE` clause. The tables share the `OrderID` column, so the condition is `O.OrderID = D.OrderID`. (Correct Answer: **d**)
* **Blank F:** The subquery returns `O.EmployeeID`. To filter employees based on the total amount of their orders, we must group the results by the employee and use the `HAVING` clause for the aggregate function `SUM()`. Therefore, `GROUP BY O.EmployeeID HAVING SUM(D.Amount) > 10000` is required. The `WHERE` clause cannot contain aggregate functions. (Correct Answer: **c**)
