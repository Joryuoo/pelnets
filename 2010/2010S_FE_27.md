---
created: 2026-09-17 18:14
status: "#philnits"
tags:
  - algorithms/2010
  - year/2010
---

# 2010S_FE_27

Which of the following is the appropriate explanation of the hash method?

a) Access method that calculates the storing address of a record from its key value using a function
b) Access method that uses a conversion table of the key values and the storing addresses of records
c) Access method that uses the storing address of the next record stored in each record
d) Direct access method that uses the key value of a record as its storing address
?
a) Access method that calculates the storing address of a record from its key value using a function

### Explanation
The hash method involves passing a search key into a mathematical formula (a hash function) to directly compute the storage address (or index) of the corresponding record.

- **Choice b** describes an index-based access method.
- **Choice c** describes a linked list or chained access method.
- **Choice d** describes direct addressing, where the key itself is directly used as the address without any calculation.

---
