---
created: 2026-09-19 00:44
status: "#philnits"
tags:
  - data-structures/2009
  - year/2009
---

# 2009S_FE_14

There is a two-dimensional array A(m, n) with m rows and n columns, and the array in physically contiguous memory addresses is accessed such that rows are stored one after another, in row-major order. Which of the following is the correct memory address of any given element A(i, j)? Here, both m and n are greater than 0, and the indices i and j range from 1 to m and n respectively. In addition, x is the memory address of the element A(1,1).

a) x+n× i+j
b) x+n× (i+1)+(j+1)
c) x+n× (i–1)+(j–1)
d) x+n× (j–1)+(i–1)
?
c) x+n× (i–1)+(j–1)

### Explanation
### Explanation
The array is stored in **row-major order**, meaning the elements of the first row are stored continuously, followed by the elements of the second row, and so on.

Given:
- Total columns = $n$
- Base address (address of $A(1,1)$) = $x$

To reach the element $A(i, j)$:
1. You must skip the first $(i-1)$ complete rows. Since each row contains $n$ elements, this amounts to skipping $n \times (i-1)$ elements.
2. In the current $i$-th row, you must skip $(j-1)$ elements to reach the $j$-th element.

The total number of elements skipped is $n \times (i-1) + (j-1)$. Thus, the correct memory address is $x + n \times (i-1) + (j-1)$.

---
