---
created: 2026-09-10 00:55
status: "#philnits"
tags:
  - object-oriented-programming/2020
  - year/2020
---
# 2020S_FE_PM_8.2

![[2020S_FE_PM_Q8_SQ2_p1.png]]
?
b, h

### Explanation
- **F**: 4 (Answer: b)
  The method `tryColumnsOf` explores the permutations of the queens recursively. It is initially called with `row = 0`. Inside the loop, it conditionally calls `tryColumnsOf(row + 1)` until `row + 1 < BOARD_SIZE`. When `row` is 3, the condition evaluates to false, stopping the recursion. Thus, the depths are `row = 0` (depth 1), `row = 1` (depth 2), `row = 2` (depth 3), and `row = 3` (depth 4). The maximum depth is 4.
- **G**: 256 (Answer: h)
  The `isValidPlacement` method is only called when `row + 1 < BOARD_SIZE` is false (in the `else if` block), which is when `row = 3`. For each recursive call, the loop executes `BOARD_SIZE` times (4 times) to try all 4 columns. This creates a tree of recursive calls. At depth 4, the number of leaf executions is 4 × 4 × 4 × 4 = 256. Therefore, the `isValidPlacement` method is invoked 256 times.
