# 2020A_FE_PM_8.1

![[2020A_FE_PM_Q8_Body_p1.png]]
![[2020A_FE_PM_Q8_Body_p2.png]]
![[2020A_FE_PM_Q8_Body_p3.png]]
![[2020A_FE_PM_Q8_Body_p4.png]]
![[2020A_FE_PM_Q8_Body_p5.png]]
![[2020A_FE_PM_Q8_SQ1_p1.png]]

?

e, b

### Explanation

**Blank A:**
In `Program 4`, the `EqualityFilter<T>` class takes a value of generic type `T`. Its constructor is defined as:
```java
EqualityFilter(___A___ value, Function<Tv, ___A___> getter) {
```
Since the class is defined as `EqualityFilter<T>`, the type for `value` and the return type of `getter` must match this type parameter `T`. Thus, `___A___` should be `T`. This corresponds to option **e**.

**Blank B:**
The `applyFilter` method uses Java streams to filter TVs based on whether the attribute extracted by the `getter` matches the stored `value`.
```java
return tvs.stream().filter(tv -> ___B___ )
```
The lambda expression needs to extract the attribute from the `tv` object using `getter.apply(tv)` and check for equality with `value` using `.equals(value)`. 
Thus, `getter.apply(tv).equals(value)` is the correct condition. This corresponds to option **b**.

---
created: 2026-09-10 08:49
status: "#philnits"
tags:
  - object-oriented-programming/2020
  - year/2020
---
