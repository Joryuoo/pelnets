---
created: 2026-09-11 21:01
status: "#philnits"
tags:
  - information-management/2012
  - year/2012
---
# 2012A_FE_35
![[2012Oct_FE_AM_Q35_p1.png]]

a) SELECT DISTINCT A.StudentID FROM A WHERE DateDiff(A.AbsenceDate,A.AbsenceDate)=1
b) SELECT DISTINCT A.StudentID FROM A, A AS A1 WHERE DateDiff(A.AbsenceDate,A1.AbsenceDate)=1
c) SELECT DISTINCT A.StudentID FROM A, A AS A1 WHERE A.StudentID = A1.StudentID AND DateDiff(A.AbsenceDate,A1.AbsenceDate)=1
d) SELECT DISTINCT A.StudentID FROM A, A AS A1 WHERE A.StudentID = A1.StudentID OR DateDiff(A.AbsenceDate,A1.AbsenceDate)=1
?
c) SELECT DISTINCT A.StudentID FROM A, A AS A1 WHERE A.StudentID = A1.StudentID AND DateDiff(A.AbsenceDate,A1.AbsenceDate)=1
### Explanation
To find students who were absent on two consecutive days, we must join the table to itself and compare the records of the **same student**.

- `A.StudentID = A1.StudentID` ensures we are comparing the absence dates of the same student.
- `DateDiff(A.AbsenceDate, A1.AbsenceDate) = 1` checks if the dates are exactly one day apart (consecutive).
- Only option **c** properly combines both conditions using `AND`.
