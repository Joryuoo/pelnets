---
created: 2026-09-19 10:02
status: "#philnits"
tags:
  - hardware/2007
  - systems-architecture/2007
  - year/2007
---

# 2007S_FE_22

Which of the following is an appropriate description concerning cache memory?

a) Cache memory is used to compensate the difference in capacity between real storage and virtual storage.
b) If a cache miss occurs in access to main memory, an interrupt occurs and data is transferred from main memory to cache memory by the program.
c) In one method, when executing a write instruction, data is written to both cache memory and main memory. In the other method, data is written only to a block in cache, and the modified block is written back into main memory only when it is replaced.
d) The need for cache memory is diminishing these days because of the significant improvement in access speeds of semiconductor memory.
?
c) In one method, when executing a write instruction, data is written to both cache memory and main memory. In the other method, data is written only to a block in cache, and the modified block is written back into main memory only when it is replaced.

### Explanation
This choice correctly describes the two primary cache writing policies: **Write-through** (where data is written simultaneously to cache and main memory) and **Write-back** (where data is written only to cache, and updated in main memory only when the cache block is evicted).

- **a) is incorrect** because cache memory compensates for the **speed** difference between the CPU and main memory, not capacity differences. Virtual memory compensates for capacity differences.
- **b) is incorrect** because cache misses are handled by the hardware's memory controller, not by triggering a software interrupt handled by a program.
- **d) is incorrect** because while memory speeds have improved, CPU speeds have improved even faster. Therefore, cache memory remains vital to bridge this growing performance gap.

---
