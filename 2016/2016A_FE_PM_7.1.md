---
created: 2026-09-11 00:12
status: "#philnits"
tags:
  - programming/2016
  - year/2016
---
# 2016A_FE_PM_7.1

![[2016A_FE_PM_Q7_Body_p1.png]]
![[2016A_FE_PM_Q7_SQ1_p1.png]]
![[2016A_FE_PM_Q7_SQ1_p2.png]]
![[2016A_FE_PM_Q7_SQ1_p3.png]]
![[2016A_FE_PM_Q7_SQ1_p4.png]]
?
c, a, d, d, e

### Explanation
**Blank A:**
The function `calc_power(int base, int power)` computes `base` raised to `power`. It multiplies `pw` by `base` exactly `power` times. The `for` loop `for (i = 0; ____A____ ; i++)` iterates `power` times when the condition is `i < power`. Thus, **c) i < power** is correct.

**Blank B:**
The function `number_digits(long input_number)` counts the digits of `input_number` using a `while` loop. The loop continues as long as there are digits left to process, meaning `input_number` has not been reduced to zero. Thus, the condition is **a) input_number != 0**.

**Blank C:**
Inside the `number_digits` loop, after counting a digit, the number must be shifted by one decimal place to remove the rightmost digit. In C, integer division by 10 achieves this. Thus, **d) input_number = input_number / 10** is the correct operation.

**Blank D:**
The `check_armstrong` function calculates the sum of each digit raised to the power of the total number of digits. `remainder` holds the current digit, and `digits` holds the total number of digits. The calculation needs `remainder` raised to `digits`, which is done via `calc_power(remainder, digits)`. Thus, **d) calc_power(remainder, digits)** is correct.

**Blank E:**
The function returns whether `n_input` is an Armstrong number. This is true if the original number (`n_input`) equals the computed `sum` of its digits raised to the power of the number of digits. The comparison `n_input == sum` evaluates to 1 (true) or 0 (false). Thus, **e) n_input == sum** is correct.
