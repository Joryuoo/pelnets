---
created: 2026-09-17 17:35
status: "#philnits"
tags:
  - programming/2011
  - algorithms/2011
  - year/2011
---

# 2011S_FE_PM_7.2

![[2011S_FE_PM_Q7_SQ2_p1.png]]
?
b, a

### Explanation
### Explanation

For **Blank E**, `path = "d/"` and `base = "/a/b/c"`. The function first initializes `bp` to point to the end of `base` (index 6). Since the last character of `base` is not `/` (`*(bp - 1) == '/'` is false), `bp` remains at index 6. The loop for `.` or `..` is skipped since `path` does not start with them. The length to copy is `bp - base = 6`. Thus, `/a/b/c` is copied to `result`. Next, a `/` is appended, and then the remaining `path` (`"d/"`) is copied. The final string is `/a/b/c/d/`, which corresponds to choice **b**.

For **Blank F**, `path = "d"` and `base = "/a/b/c/"`. The function initializes `bp` to the end of `base` (index 7). Since the last character is `/`, `bp` is decremented to index 6. The loop for `.` or `..` is skipped. The length to copy is `bp - base = 6`. Thus, `/a/b/c` is copied to `result`. A `/` is appended, making it `/a/b/c/`. Then the remaining `path` (`"d"`) is copied. The final string is `/a/b/c/d`, which corresponds to choice **a**.

---
