---
created: 2026-09-07 20:06
status: "#philnits"
tags:
  - algorithms/2026
  - year/2026
---

# 2026S_FE-B_6

![[2026S_FE-B_Q6_full.png]]
?
d) b, sqrt(-val)

### Explanation
The provided algorithm determines the roots of a quadratic equation. The variable `val` represents the discriminant-like term $b^2 - c$. 

- **Case 1 (`val = 0`)**: The roots are real and equal (a double root). The math formula dictates the root is $b$. Thus, the missing output value **A** is simply `b`.
- **Case 2 (`val > 0`)**: The roots are $b \pm \sqrt{b^2 - c}$. Since `val` $= b^2 - c$, `tmp` evaluates to $\sqrt{val}$, and the roots are correctly computed as `b + tmp` and `b - tmp`.
- **Case 3 (`val < 0`)**: The roots are imaginary, expressed as $b \pm i\sqrt{c - b^2}$. Since `val` $= b^2 - c$, the expression under the square root becomes $-val$. Therefore, we need to calculate $\sqrt{-val}$. The missing assignment for `tmp` (**B**) should be `sqrt(-val)`.

Hence, the correct combination is `b` and `sqrt(-val)`, corresponding to answer **d**.
