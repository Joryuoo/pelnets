---
created: 2026-09-07 20:06
status: "#philnits"
tags:
  - algorithms/2026
  - year/2026
---

# 2026S_FE-B_5

![[2026S_FE-B_Q5_full.png]]
?
e) B, C, C, A,

### Explanation
Let's trace the execution stack when `proc1()` is called:

1. `proc1()` starts and makes a call to `proc2()`.
2. Inside `proc2()`:
   - It outputs `"B, "`.
   - It then makes a call to `proc3()`.
3. Inside `proc3()` (called from `proc2`):
   - It outputs `"C, "`.
   - Function finishes and returns control to `proc2()`.
4. `proc2()` finishes and returns control back to `proc1()`.
5. Back in `proc1()`, it makes a direct call to `proc3()`.
6. Inside `proc3()` (called from `proc1`):
   - It outputs `"C, "`.
   - Function finishes and returns control to `proc1()`.
7. Finally, `proc1()` executes its last statement:
   - It outputs `"A, "`.

Following the sequence of outputs, we get: **B, C, C, A,**.
