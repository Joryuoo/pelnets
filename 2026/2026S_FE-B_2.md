---
created: 2026-09-07 20:06
status: "#philnits"
tags:
  - algorithms/2026
  - year/2026
---

# 2026S_FE-B_2

![[2026S_FE-B_Q2_full.png]]
?
d) b, b, d

### Explanation
The function `f` has a specification bug because the first `if` statement is followed by a completely separate `if` statement, instead of using an `elseif`.

Let's trace the execution for the given inputs:
1. **`f(92)`**: 
   - `argval > 90` is true $\rightarrow$ `retvar` becomes `"a"`.
   - The next `if` statement is evaluated: `argval > 70` is true $\rightarrow$ `retvar` is overwritten with `"b"`.
   - The function returns `"b"`. (This is answer **A**)
2. **`f(72)`**:
   - `argval > 90` is false.
   - The next `if` statement is evaluated: `argval > 70` is true $\rightarrow$ `retvar` becomes `"b"`.
   - The function returns `"b"`. (This is answer **B**)
3. **`f(-10)`**:
   - `argval > 90` is false.
   - `argval > 70` is false.
   - `elseif (argval > 60)` is false.
   - `elseif (argval <= 60)` is true $\rightarrow$ `retvar` becomes `"d"`.
   - The function returns `"d"`. (This is answer **C**)

Thus, A, B, and C are `b`, `b`, and `d` respectively.
