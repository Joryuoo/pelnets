---
created: 2026-09-11 09:39
status: "#philnits"
tags:
  - cybersecurity/2015
  - year/2015
---
# 2015A_FE_PM_1.1

![[2015A_FE_PM_Q1_Body_p1.png]]
![[2015A_FE_PM_Q1_SQ1_p1.png]]
![[2015A_FE_PM_Q1_SQ1_p2.png]]
![[2015A_FE_PM_Q1_SQ1_p3.png]]

?
e

### Explanation

Based on the scenario description and the diagrams provided:

1.  **Blank (1):** The scenario explicitly states that "the **SHA-1** (Secure Hash Algorithm-1) is used to generate a 160-bit hash code of the message." In Figure 1, the operation that takes the *Email message* as input and outputs the *Hash Code* is marked as **(1)**. Therefore, **(1) is SHA-1**.
2.  **Blank (2):** The text explains that "the hash code is then encrypted with **RSA algorithm** using the sender’s private key" to create the digital signature. In Figure 1, the *Hash Code* and the *Sender's private key* are processed by block **(2)** to produce the *Digital Signature*. Therefore, **(2) is the RSA algorithm**.
3.  **Blank (3):** The text mentions that "the ZIP algorithm... is used to compress the message before it is sent." Figure 2 shows operations at the receiver site. Block **(3)** takes the *Compressed message* and converts it back into the *Email message*. This is the reverse of compression, so **(3) must be the Decompression algorithm**.

Looking at the options, **choice e)** correctly matches all three operations:
*   (1) SHA-1
*   (2) RSA algorithm
*   (3) Decompression algorithm

**Why other choices are incorrect:**
*   **Choices a, b, c, f:** These incorrectly identify the algorithms for (1) and/or (2). As established by the text, SHA-1 handles the hashing, and RSA handles the encryption of the hash.
*   **Choice d:** While it correctly identifies (1) and (2), it incorrectly labels (3) as the "Compression algorithm". At the receiver site, the incoming compressed data must be decompressed to retrieve the original email message.
