---
created: 2026-09-11 20:01
status: "#philnits"
tags:
  - programming
  - year/2013
---
# 2013S_FE_PM_7.2

![[2013Apr_FE_PM_Q7_SQ2_p1.png]]
?
D=d, E=d

### Explanation

**Blank D:**
The question asks for the function that requires a pointer to an array of integers as its input parameter. 
Looking at the function prototypes in the code:
- `int** createArray(void)` takes no parameters (`void`).
- `void recordScores(int** arrayOfScores)` takes a pointer to a pointer to an integer (`int**`). In this program, this represents an array of pointers.
- `void findTime(int** arrayOfScores)` also takes a pointer to a pointer to an integer (`int**`).
- `int scoreMinimum(int* playerPtr)` takes a pointer to an integer (`int*`). In C, passing an array of integers to a function is typically done by passing a pointer to its first element (an `int*`). In this program, `playerPtr` points to an array of scores for a single player.

Therefore, **d) int scoreMinimum (int* playerPtr)** is the correct answer.

**Blank E:**
The question describes a scenario where an array of pointers pointing to 8 arrays of integers is created after executing `createArray(void)`. It asks which variable was assigned the integer value 8.

Looking at the `createArray()` function:
```c
printf("\nEnter the number of players who practiced today: "); 
scanf("%d", &rowNum); 
```
The program prompts the user for the number of players and stores this input in the variable `rowNum`. This value is then used in the condition of the `while (playersCtr < rowNum)` loop to determine how many inner arrays (representing lap times for each player) are dynamically allocated. 

If 8 arrays of integers were created (meaning there are 8 players), the user must have inputted 8, meaning the variable `rowNum` was assigned the value 8.

Therefore, **d) rowNum** is the correct answer.
