---
created: 2026-09-11 20:01
status: "#philnits"
tags:
  - programming
  - year/2013
---

# 2013S_FE_PM_7.1

![[2013Apr_FE_PM_Q7_SQ1_p1.png]]
![[2013Apr_FE_PM_Q7_SQ1_p2.png]]
![[2013Apr_FE_PM_Q7_SQ1_p3.png]]
![[2013Apr_FE_PM_Q7_SQ1_p4.png]]
?

A=c, B=b, C=d

### Explanation

- **Blank A (c):** The pointer `arrayOfScores` is declared as `int**` (a pointer to an `int` pointer), meaning it will hold an array of pointers for each player's scores. In functions like `recordScores` and `findTime`, loops iterate using the condition `while (arrayOfScores[...] != NULL)`. To correctly terminate these loops, the array must be able to hold all `rowNum` players plus one extra slot at the end for the `NULL` sentinel pointer. Since the array stores pointers, the size of each element must be `sizeof(int*)`. Therefore, `(int**) calloc(rowNum + 1, sizeof(int*))` is correct.
- **Blank B (b):** The `createArray` function needs to return the fully constructed 2D array back to `main`. The function's return type is `int**`, and the variable holding the 2D array is `arrayOfScores`. Thus, `return arrayOfScores;` is the correct statement.
- **Blank C (d):** The `scoreMinimum` function expects an argument of type `int*` (`int scoreMinimum(int* playerPtr)`), which represents a single player's array of lap scores. Inside the `findTime` function, the loop index variable `lapScore` is used to iterate through the players. Since `arrayOfScores` is an `int**`, indexing it as `arrayOfScores[lapScore]` gives an `int*` for the current player's lap scores, passing the exact required data type to `scoreMinimum`.
