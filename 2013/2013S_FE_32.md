---
created: 2026-09-11 15:10
status: "#philnits"
tags:
  - information-management/2013
  - year/2013
---
# 2013S_FE_32
![[2013Apr_FE_AM_Q32_p1.png]]
?
b)
### Explanation
The SQL statement uses the `UNION` operator to combine the results of two queries:

1. `SELECT StaffID, BranchID FROM Staff WHERE BranchID = 'B03'`
   - This returns rows for `S03`, `S04`, `S05`.
2. `SELECT StaffID, BranchID FROM Staff WHERE Salary > 5000`
   - This returns rows for `S04` (Salary 8000) and `S07` (Salary 6000).

The `UNION` operator removes duplicate rows by default (unlike `UNION ALL`). The record for `S04` is present in both intermediate results, so it will only appear once in the final output. The final table will contain the records for `S03`, `S04`, `S05` (from `B03`), and `S07` (from `B08`).
