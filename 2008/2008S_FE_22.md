---
created: 2026-09-19 09:33
status: "#philnits"
tags:
  - hardware/2008
  - systems-architecture/2008
  - year/2008
---

# 2008S_FE_22

Q22. Which of the following appropriately describes cache memory?  

a) An interrupt occurs if a cache miss is detected when the main memory is accessed, and the program transfers data from the main memory to the cache memory.  
b) The cache memory is used to make up the difference of capacity between the real memory and the virtual memory.  
c) The demand for cache memory is decreasing due to the rapid increase in the access speed of semiconductor memory.  
d) Two methods exist: one is to rewrite both the cache and main memory when a write instruction is executed; the other is to rewrite only the cache memory while the main memory is not rewritten until the data is removed from the cache memory.
?
d) Two methods exist: one is to rewrite both the cache and main memory when a write instruction is executed; the other is to rewrite only the cache memory while the main memory is not rewritten until the data is removed from the cache memory.

### Explanation
Cache memory acts as a high-speed buffer between the CPU and main memory to bridge the gap in access speeds. Choice **d)** accurately describes the two primary cache write policies: **write-through** (updating both cache and main memory simultaneously) and **write-back** (updating only the cache immediately, and writing to main memory later when the block is evicted).

- **a)** is incorrect because a cache miss is handled transparently by the hardware cache controller, not via a software interrupt.
- **b)** is incorrect; virtual memory makes up for capacity differences between RAM and disk, whereas cache addresses speed differences.
- **c)** is incorrect; the speed gap between CPUs and main memory is still significant, keeping cache highly necessary.

---
