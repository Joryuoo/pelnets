---
created: 2026-09-19 09:17
status: "#philnits"
tags:
  - information-management/2008
  - year/2008
---

# 2008A_FE_64

Which of the following appropriately describes a group function of SQL?

a) A group function returns a group of results from one row.
b) A group function returns a single result row per group for a set of queried rows.
c) A group function returns multiple result rows per group for a set of queried rows.
d) A group function returns one result from each row in the table.
?
b) A group function returns a single result row per group for a set of queried rows.

### Explanation
### Explanation
A group function (or aggregate function) in SQL, such as `SUM()`, `AVG()`, or `COUNT()`, operates on a set of rows grouped together and returns a single aggregate value (one result row) for each group.

---
