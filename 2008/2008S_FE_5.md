---
created: 2026-09-19 09:33
status: "#philnits"
tags:
  - digital-logic/2008
  - year/2008
---

# 2008S_FE_5

![[2008S_FE_AM_Q5_full.png]]
?
c)

### Explanation
### Explanation
The truth table provided for `x # y` outputs `True` only when both `x` and `y` are `False`. In all other cases (where at least one input is `True`), it outputs `False`.

This behavior corresponds to the **NOR** (NOT OR) logic gate, which can be expressed as `NOT (x OR y)`.

Using De Morgan's laws, `NOT (x OR y)` is mathematically equivalent to `(NOT x) AND (NOT y)`. 
This matches choice c).

---
