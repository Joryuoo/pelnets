---
created: 2026-09-09 23:12
status: "#philnits"
tags:
  - cybersecurity/2021
  - information-management/2021
  - year/2021
---
# 2021A_FE_AM_43
An SQL injection attack caused the SQL statement below to be executed. Which of the following does the SQL statement do? Here, the accounts table contains account information in each row.

SELECT * FROM accounts WHERE username='1' or '1’='1'; DROP TABLE accounts;

a) It creates a new user ‘1’.
b) It creates a pop-up box that shows the first username in the “accounts” table.
c) It selects all the records in the “accounts” table and deletes the “accounts” table from the database.
d) It selects one record from the “accounts” table and drops the rest of the records in the table.
?
c) It selects all the records in the “accounts” table and deletes the “accounts” table from the database.
### Explanation
The provided SQL statement actually executes two separate commands separated by a semicolon (;).
1. `SELECT * FROM accounts WHERE username='1' or '1’='1';`: This evaluates to true for all rows because `'1'='1'` is a tautology. Thus, it selects all records in the `accounts` table.
2. `DROP TABLE accounts;`: This command immediately deletes (drops) the entire `accounts` table from the database.
Therefore, it selects all records and then deletes the table.
