---
created: 2026-09-10 00:00
status: "#philnits"
tags:
  - object-oriented-programming/2021
  - year/2021
---
# 2021S_FE_PM_8.2

![[2021S_FE_PM_Q8_SQ2_p1.png]]

?

a

### Explanation
**G:** In Program 4, the `removeRestAfter` method iterates backward from `newestMarker.previous` to find the specified `webPage`. Once found at `node`, all nodes after it must be removed. To accomplish this truncation, the `node` must become the new last element in the doubly linked list. 
This is done by linking `node` directly to the tail sentinel `newestMarker`. 
So, `node.next` should point to `newestMarker`.
`newestMarker.previous` is then updated to point to `node` on the next line.
Thus, **G** is `newestMarker` (a).
