---
created: 2026-09-10 00:00
status: "#philnits"
tags:
  - object-oriented-programming/2021
  - year/2021
---
# 2021A_FE_PM_8.1

![[2021A_FE_PM_Q8_Body_p1.png]]
![[2021A_FE_PM_Q8_Body_p2.png]]
![[2021A_FE_PM_Q8_Body_p3.png]]
![[2021A_FE_PM_Q8_Body_p4.png]]
![[2021A_FE_PM_Q8_Body_p5.png]]
![[2021A_FE_PM_Q8_SQ1_p1.png]]
![[2021A_FE_PM_Q8_SQ1_p2.png]]

?

b, c, a, a, e

### Explanation

**Blank A:**
In `[Program 2]`, `enum DigitKey ___A___ Key` defines the `DigitKey` enum. `Key` is defined in `[Program 1]` as `public interface Key`. In Java, an enum implements interfaces using the keyword `implements`. Therefore, `DigitKey implements Key`. The correct answer is **b**.

**Blanks B and C:**
In `[Program 2]`, the method `operateOn` for `DigitKey` updates the numeric value currently being entered by combining the previous digits with the newly pressed digit.
```java
public void operateOn(Stack<Integer> stack) { 
   stack.push(___B___ * 10 + ___C___); 
} 
```
To shift the previously entered value by one decimal place to the left, we need to extract it from the top of the stack using `stack.pop()`. Then, we add the numeric value of the newly pressed digit. The enum constants `DIGIT0` to `DIGIT9` represent values 0 to 9, which correspond exactly to their `ordinal()` values (e.g., `DIGIT0.ordinal()` is 0, `DIGIT9.ordinal()` is 9). Thus, the correct sequence is `stack.pop()` (Blank B) and `ordinal()` (Blank C). The correct answers are **c** and **a**.

**Blank D:**
In `[Program 3]`, `OperationKey` enumerates mathematical operations. For `ADD`, `SUBTRACT`, `MULTIPLY`, and `DIVIDE`, the `operator` field holds a valid lambda function for computation. For `EQUAL` and `CLEAR`, the `operator` is `null`. The `operateOn` method uses this `operator` to apply the calculation to the top two values from the stack.
```java
public void operateOn(Stack<Integer> stack) { 
   if (___D___ ) { 
      Integer val2 = stack.pop(); 
      Integer val1 = stack.pop(); 
      stack.push(operator.apply(val1, val2)); 
   } 
} 
```
To avoid a `NullPointerException` when processing keys like `EQUAL` or `CLEAR`, we must ensure that `operator` is not null. Thus, the condition should be `operator != null`. The correct answer is **a**.

**Blank E:**
In `[Program 5]`, the `map` needs to store associations between characters (like '+', '-', '0', '1') and their corresponding enumeration instances from either `OperationKey` or `DigitKey`.
```java
Map<Character, ___E___> map = new HashMap<>(); 
```
Since both `OperationKey` and `DigitKey` implement the `Key` interface, their common supertype that encapsulates both functionalities is `Key`. By typing the map values as `Key`, we can seamlessly store instances from both enums. The correct answer is **e**.
