---
created: 2026-09-11 14:24
status: "#philnits"
tags:
  - operating-systems/2014
  - year/2014
---
# 2014S_FE_23
Among the explanations of task scheduling methods, which of the following has the highest possibility that a specific task continues to wait for the allocation of CPU time?

a) Each task is added to the end of the Ready queue in order of arrival into the system, and the CPU time is always allocated to the first task in the Ready queue.
b) Each task is executed in the order listed in the Ready queue. Once a fixed period of time has elapsed, the execution of the task is suspended and it is added to the end of the Ready queue.
c) The priority of each task is determined, and each task is executed in high to low priority order. The priority is increased gradually according to the length of the waiting time until the CPU time is allocated.
d) The processing is executed from the task with the estimated shortest processing time. The subsequent task is started when the processing being executed is either completed or has been suspended for some reason.
?
d) The processing is executed from the task with the estimated shortest processing time. The subsequent task is started when the processing being executed is either completed or has been suspended for some reason.
### Explanation
Option **d)** describes the Shortest Job First (SJF) scheduling method. In this method, longer tasks might never get executed if new, shorter tasks continually arrive in the queue. This endless waiting is known as **starvation**.
- **a)** describes First-Come, First-Served (FCFS), where tasks are executed in arrival order, ensuring every task eventually runs.
- **b)** describes Round Robin scheduling, ensuring all tasks get equal CPU time slices.
- **c)** uses an aging technique, which specifically prevents starvation by increasing priority over time.
