---
created: 2026-09-07 18:51
status: "#philnits"
tags:
  - algorithm/2025
  - year/2025
---

# 2025A_FE-B_1

![[2025A_FE-B_Q1_full.png]]
?
a) array[i] mod 2 = 0, i <- i + 1

### Explanation
The problem asks to calculate the sum of even elements from an array within a given index range $k$ to $m$. 
- The variable `s` acts as the accumulator for the sum, initialized to $0$. 
- The loop iterates over indices $i$ from $k$ to $m$.
- To identify if an element is even, we use the modulo operator. The condition for an even number is `array[i] mod 2 = 0`. This fills the blank `____A____`.
- To progress the loop to the next element, the index `i` must be incremented. Therefore, `i <- i + 1` fills the blank `____B____`.

Matching these conditions with the given choices, option **a)** is the correct answer.
