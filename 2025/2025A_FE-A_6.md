Created: 2026-09-04 18:45
Category: #appropriate-category
Status: #philnits

# 2025A_FE-A_6

![[2025A_FE-A_Q6_full.png]]
?
b) 2

### Explanation
1. The flowchart initializes $a = 56$ and $b = 48$.
2. Point A is located on the main flow path, just before the operation $r \leftarrow a \bmod b$. This means A is passed upon the initial entry into the loop, and every time the loop repeats.
3. **Initial entry (1st pass of A):**
   - $r \leftarrow 56 \bmod 48 = 8$
   - $a \leftarrow 48$
   - $b \leftarrow 8$
   - The condition $r = 0$ is checked. Since $r = 8$, the flow goes to "No", looping back up to the main flow path.
4. **Second entry (2nd pass of A):**
   - $r \leftarrow 48 \bmod 8 = 0$
   - $a \leftarrow 8$
   - $b \leftarrow 0$
   - The condition $r = 0$ is checked. Since $r = 0$, the flow goes to "Yes".
5. The algorithm prints $a$ (which is 8) and ends.

Therefore, point A is passed a total of 2 times (once during the initial flow, and once from the loop back).

---
# References
- Algorithms and Flowcharts (Greatest Common Divisor / Euclidean Algorithm)
