---
created: 2026-09-04 18:45
status: "#philnits"
tags:
  - object-oriented-programming/2025
  - year/2025
---
# 2025A_FE-A_7

In object-oriented programming, which of the following is the concept that refers to a class that defines a method with the same name and type signature as a method in its superclass?

a) Hiding
b) Inheritance
c) Overloading
d) Overriding
?
d) Overriding

### Explanation
In object-oriented programming, when a subclass provides a specific implementation for a method that is already defined in its superclass with the exact same name, return type, and parameters (type signature), it is called **Overriding**. This allows the subclass to exhibit behavior specific to its type while sharing the same interface as its parent class.

| Concept | Description |
| :--- | :--- |
| **Hiding** | Usually refers to variable hiding (shadowing) or hiding static methods, not instance methods. |
| **Inheritance** | The mechanism by which a class acquires properties and behaviors from a parent class. |
| **Overloading** | Occurs when multiple methods in the same class share the same name but have different parameter lists (different signatures). |
| **Overriding** | Redefining a superclass's method in a subclass with the same signature. |

---
# References
- Object-Oriented Programming (Polymorphism)
