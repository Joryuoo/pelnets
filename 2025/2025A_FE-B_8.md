---
created: 2026-09-07 18:51
status: "#philnits"
tags:
  - data-structures/2025
  - year/2025
---

# 2025A_FE-B_8

![[2025A_FE-B_Q8_p1.png]]
![[2025A_FE-B_Q8_p2.png]]
?
b) tos <- tos + 1, item <- stck[tos] ; tos <- tos - 1

### Explanation
The code defines stack operations using an array `stck` of size 10 and a top-of-stack pointer `tos`. `tos` tracks the index of the topmost element and is initialized to $0$. Since the array index starts at 1, the `push` and `pop` operations must increment and decrement `tos` appropriately.

**For `push(item)`:**
- To add a new item to the stack, the pointer `tos` must first be moved to the next available empty slot in the array. Since it starts at $0$, the first pushed item should go to index $1$. 
- Therefore, `tos` must be incremented *before* storing the item: `tos <- tos + 1`. This fills `____A____`.
- The new item is then placed at `stck[tos] <- item`.

**For `pop()`:**
- To remove and return the topmost item from the stack, the item currently located at the `tos` index is read and saved into the variable `item`. 
- The pointer `tos` is then decremented to point to the new topmost element.
- Therefore, `____B____` must execute `item <- stck[tos]` followed by `tos <- tos - 1`.

This matches choice **b)**.
