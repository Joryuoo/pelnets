Created: 2026-09-04 19:30
Category: #database #transaction
Status: #philnits

# 2025S_FE-A_22

Q22. Which of the following is an appropriate explanation of the granularity of locks? 
a) When large granularity is set for a large number of transactions to update the same data, the number of transactions that can be executed simultaneously increases. 
b) When large granularity is set for all data in a table to be referenced, data referenced from other transactions can remain undisturbed. 
c) When large granularity is set for data to be updated, the waiting times for other transactions increase, and the total throughput declines. 
d) When large granularity is set, the number of data items included increases, and the number of locks that one transaction makes increases. 
?
c) When large granularity is set for data to be updated, the waiting times for other transactions increase, and the total throughput declines. 

### Explanation
- **Lock Granularity**: Refers to the size of the data item that is locked (e.g., row, page, table, database).
- **Large Granularity** (e.g., table lock):
  - Requires fewer locks in total, which reduces system overhead and the number of locks a single transaction makes.
  - However, it drastically limits concurrency. If a whole table is locked for an update, other transactions trying to access or update different rows in the same table must wait.
- Therefore, setting a large granularity for data updates increases waiting times for other transactions, reducing the overall system throughput.

---
# References
- Database Lock Granularity
